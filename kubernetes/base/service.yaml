apiVersion: v1
kind: Service
metadata:
  name: pet-adoption
  labels:
    app: pet-adoption
spec:
  type: ClusterIP
  selector:
    app: pet-adoption
  ports:
    - name: http
      port: 80
      targetPort: http